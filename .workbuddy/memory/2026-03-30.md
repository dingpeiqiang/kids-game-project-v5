# 2026-03-30 工作日志

## Phaser 全项目统一改为 CDN 加载（2026-03-30）

将项目中所有子工程的 Phaser 加载方式统一为 CDN，不再通过 npm 打包进 bundle。

## 修复 Phaser CDN 报错（2026-03-30）

**问题**：`GameScene.ts` 报错 `does not provide an export named 'default'`

**根因**：`package.json` 中虽已移除 phaser，但 `node_modules/phaser` 目录还在（之前 npm install 留下的），Vite 优先找到了本地 `node_modules/phaser` 而非走 CDN 全局变量。`optimizeDeps.exclude` 只阻止预构建，不阻止运行时路径解析。

**解决**：直接删除各项目 `node_modules/phaser` 目录：
- puzzle ✅、snake ✅、plane-shooter ✅、kids-game-frontend ✅（kids-game-frame-factory 本来就没有）

**结论**：仅靠 `optimizeDeps.exclude` 不够，必须同时物理删除 node_modules 中的包。

## 补修 plane-shooter 漏改项（2026-03-30）

plane-shooter 的 `PhaserGame.vue` 和 `GameScene.ts` 在上一轮改造中被遗漏，仍保留了 `import Phaser from 'phaser'`，导致 Vite pre-transform 报错 `Failed to resolve import "phaser"`。

已修复：删除两个文件中的 import 语句，改为 CDN 全局变量方式。
全局扫描确认：项目所有 `.ts`/`.vue` 文件中已无 `import Phaser from 'phaser'`。

### 修改清单

| 子项目 | index.html CDN | vite externals | package.json 移除 phaser | import 改为全局变量 |
|--------|----------------|----------------|--------------------------|---------------------|
| puzzle | ✅ 新增 3.90.0 | ✅ 新增 | ✅ | ✅ PhaserGame.vue + GameScene.ts |
| game-template | ✅ 新增 3.90.0 | ✅ 新增 | ✅ | ✅ PhaserGame.vue + GameScene.ts |
| snake | ✅ unpkg→jsdelivr 3.90.0 | 已有 | ✅ | SnakeLevelOrchestrator.ts |
| plane-shooter | ✅ 3.70.0→3.90.0 | 已有 | 已无 | 已有 |
| kids-game-frontend | ✅ 3.87.0→3.90.0 | 已有 | ✅ | 已有 |
| kids-game-frame-factory/src | — | — | peerDep保留 | ✅ LevelOrchestrator.ts + LevelResourceLoader.ts |

### 统一规范
- CDN 地址：`https://cdn.jsdelivr.net/npm/phaser@3.90.0/dist/phaser.min.js`
- vite externals：`external: ['phaser']` + `globals: { phaser: 'Phaser' }`
- 全局变量声明：`global.d.ts` 中 `declare const Phaser: typeof import('phaser').default`
- puzzle/game-template 新建了 `src/global.d.ts`

### 注意
- `kids-game-frame-factory/package.json` 的 `peerDependencies` 保留（作为框架库声明宿主需提供）
- snake 旧有的 `src/global.d.ts` 和 `PhaserGame.ts` 内 `declare const Phaser: any` 保持不变
