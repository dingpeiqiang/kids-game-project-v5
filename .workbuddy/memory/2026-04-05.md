# 2026-04-05

## catch-the-cat 游戏优化完成

### 新增功能
1. **难度级别系统**：四种难度（简单/普通/困难/专家），通过 `difficulty` 参数配置
2. **计分系统**：智能计分算法，基于步数、剩余空格和难度倍数
3. **音效开关按钮**：🔊/🔇 按钮控制音效
4. **动画效果**：
   - 方块悬停高亮 + 放大效果
   - 墙壁放置弹跳动画
   - 胜利光环特效
   - 分数变化缩放动画

### 新增文件
- `src/sprites/scoreDisplay.ts` - 计分显示组件
- `src/sprites/soundToggleButton.ts` - 音效开关按钮

### 修改文件
- `src/game.ts` - 添加 `DIFFICULTY_CONFIG` 和 `DifficultyLevel` 类型
- `src/scenes/mainScene.ts` - 添加计分、难度、动画逻辑
- `src/sprites/block.ts` - 添加悬停和放置动画
- `OPTIMIZATION.md` - 更新文档

### 使用方式
```javascript
// 设置难度
new CatchTheCatGame({
    w: 11,
    h: 11,
    r: 20,
    difficulty: 'hard'  // easy | normal | hard | expert
});
```

### Vite 模式优化（2026-04-05 18:50）

**目标**：默认使用 Vite 模式，`npm run dev` 启动游戏

**已完成**：
1. `package.json` - 简化为只有 Vite 依赖
2. `vite.config.ts` - 优化配置（端口 5173，CDN external）
3. `index.html` - 简化结构，移除内联脚本
4. `src/index.ts` - 添加自动初始化逻辑
5. 删除 `webpack.config.js`（不再使用）

**启动命令**：
```bash
cd kids-game-house/games/catch-the-cat
npm install
npm run dev
```

**访问地址**：`http://localhost:5173`

---

## car-on-curved-terrain 地图优化（2026-04-05 19:30）

### 新增文件
- `src/scripts/objects/terrainGenerator.ts` - 程序化地形生成器（Value Noise + FBM）
  - 5种地形风格：gentle / hilly / mountain / canyon / desert
  - 支持种子参数确保可复现
  - 自动生成两段连续地形 + 桥梁位置
- `src/scripts/objects/mapDecorator.ts` - 地图装饰系统（Graphics 程序化绘制）
  - 云朵（视差滚动）、远山、树木、灌木、石头、花朵
  - 5种装饰主题：meadow / forest / desert / snow / canyon
- `src/scripts/objects/finishFlag.ts` - 终点旗帜（棋盘格终点线 + 红旗动画）

### 修改文件
- `src/scripts/objects/levelManager.ts` - 重写，使用程序化地形生成5关
  - 关卡：新手之路(1000m) / 山地挑战(1500m) / 极限越野(2000m) / 沙漠公路(2500m) / 峡谷穿越(3000m)
  - 每关有独特 terrainStyle + decorStyle
  - 桥梁位置由程序化计算
- `src/scripts/scenes/mainScene.ts` - 集成装饰系统、终点旗帜、背景风格切换
- `src/scripts/scenes/backgroundScene.ts` - 重写，多层视差滚动 + 天空渐变 + 太阳 + 远山
- `src/scripts/objects/levelSelectUI.ts` - 适配5关网格布局
- `src/scripts/objects/particleEffects.ts` - 兼容 Phaser 3.16 粒子 API

### 启动
```bash
cd kids-game-house/games/car-on-curved-terrain
npm run dev
```
访问 `http://localhost:3000`

### Bug 修复（2026-04-05 08:20）
- **错误**：`BackgroundScene.createMidGround` 中使用了 `quadraticCurveTo`，Phaser 3.16 不支持此方法
- **修复**：改用 7 段折线模拟圆弧丘陵，使用 `Math.sin` 替代 `Math.random` 保证确定性
- **结论**：Phaser 3.16 Graphics 不支持 `quadraticCurveTo` / `bezierCurveTo`，需用折线替代

### 性能优化（2026-04-05 08:20）
**问题**：游戏严重卡顿，原因是 mapDecorator 每个装饰元素独立一个 Graphics 对象（200+个），大量 draw call
**修复方案**：
1. `mapDecorator.ts` — 重写为 3 个 Graphics 层（farLayer/midLayer/nearLayer），所有装饰绘制到对应层
   - 新增 SeededRandom LCG 伪随机类，避免 Math.random() 不确定性
   - 树木上限 60 棵，花朵上限 80 个，灌木上限 40 个，避免极端数量
2. `backgroundScene.ts` — 所有层改为 scrollFactor(0) + update() 里手动平移，避免超宽 Graphics 跟随世界坐标渲染
3. `mainScene.ts` — worldWidth 上限 8000，密度降为 easy:0.5/medium:0.4/hard:0.3

---

## math-attack 迁移至 Vite 模式（2026-04-05）

**原状态**：Webpack 5 + Babel + live-server，纯 ES Module JS 项目

**迁移内容**：
1. `package.json` — 移除 webpack/babel/live-server，改为 `vite@^5.2.0`，脚本改为 `dev/build/preview`，添加 `"type":"module"`
2. `vite.config.js` — 新建，Phaser 设为 external（CDN 加载），publicDir 指向 public，端口 3000
3. `index.html` — Phaser CDN 升级到 3.90.0，`<script>` 改为 `type="module" src="/main.js"`
4. `public/` 目录 — 新建，将 `assets/`（图片/音频）和 `favicon.ico` 复制进去（Vite 静态资源约定）
5. `webpack.config.js` — 删除

**启动命令**：
```bash
cd kids-game-house/games/math-attack
npm install
npm run dev
# 访问 http://localhost:3000
```

**说明**：原 `assets/` 目录保留在根目录（历史兼容），`public/assets/` 是 Vite 生产构建时使用的副本

### Phaser 3.90 兼容性修复（2026-04-05 13:00）

**问题 1**：`Frame "2" not found in texture "rocket"`
- **原因**：rocket.png 实际尺寸 248×200，每帧 124×200，只有 **2 帧（0~1）**，但 `createAnimation` 传入 `end:2` 越界
- **修复**：`scenes/preload.js` 中改为 `end: 1`

**问题 2**：`this.tweens.timeline is not a function`
- **原因**：`tweens.timeline()` 在 Phaser 3.60+ 已彻底移除
- **修复**：`utils/common.js` 中 `flashElement` 改用 `tweens.add({ yoyo:true, repeat:N })` 实现相同闪烁效果

**Phaser 版本升级注意事项**（适用本项目所有游戏）：
- `tweens.timeline()` → 用 `tweens.add({ yoyo, repeat })` 替代
- spritesheet `end` 帧号必须 ≤ 实际帧数-1（3.90 严格校验）
