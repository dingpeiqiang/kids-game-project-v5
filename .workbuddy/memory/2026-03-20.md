# 2026-03-20 工作日志

### 修复：游戏前后大小比例不一致
- **问题**：浏览器没有缩放，但游戏前和游戏后大小有变化
- **原因**：UI 响应式参数只在 StartView 的 onMounted 时初始化，其他页面没有统一初始化。游戏过程中 PhaserGame 用的是游戏画布尺寸，与 window.innerWidth/innerHeight 不同
- **修复**：在 App.vue 中添加全局路由守卫 `router.beforeEach`，每次路由切换时统一使用 window 尺寸初始化 UI 参数
- 同时添加 window.resize 监听器，实时更新 UI 参数

---

## 贪吃蛇 GTRS 规范适配 - 收尾工作

### 修复：成绩上报 message 为 undefined
- **问题**：控制台显示 `成绩上报失败: undefined`
- **原因**：`platformApi.ts` 中 `response.data.message` 可能为空
- **修复**：添加默认值 `response.data.message || '成绩上报失败 (code: ' + response.data.code + ')'`

### 修复：游戏结束后首页尺寸变小
- **问题**：贪吃蛇游戏结束后回到首页，首页尺寸变小了
- **原因**：
  1. `uiResponsive.ts` 中的全局变量在游戏时被 `PhaserGame` 修改
  2. 首次加载 StartView 时 `initUIParams` 没有被调用
- **修复**：
  1. 在 `StartView.onMounted` 中调用 `initUIParams` 初始化
  2. 添加 `window.resize` 监听器实时更新 UI 参数
  3. 设计基准从 `720×1280` 改为 `720×720`（正方形，更适配宽屏）
  4. 大屏幕（宽度 > 1024）最小缩放设为 0.5

### 修复：主题重复加载问题
- **问题**：贪吃蛇反复调用 `/api/theme/download`
- **原因**：主题被加载两次
  1. `themeStore.init()` → `loadThemeFromBackend()` (第1次)
  2. `PhaserGame.start()` → `loadTheme()` (第2次)
- **修复**：优化 `PhaserGame.loadTheme()` 逻辑
  - 优先使用 `themeStore.gtrsRawJson`（已校验通过）
  - 仅当 `gtrsRawJson` 为空时才从后端获取
  - 控制台输出 `♻️ 复用 themeStore 已加载的 GTRS 主题`

### 完成内容
1. **theme.ts `loadThemeFromBackend` 改造**
   - 动态 import `validateGTRSTheme`，加入严格 GTRS 校验
   - 从 `gtrsTheme.globalStyle` 提取颜色（`primaryColor` / `secondaryColor` / `bgColor` / `textColor`）
   - 从 `gtrsTheme.resources.audio` 提取音量配置到 `ThemeConfig.sounds`
   - 校验失败返回 `false`，拒绝加载脏数据
   - lint 检查：0 错误 ✅

2. **PhaserGame.ts 完整 GTRS 适配**（上一轮已完成，本轮确认）
   - `let GTRS: GTRSTheme` 可变对象，支持运行时替换
   - `deepMerge()` / `normalizeSrcPaths()` / `overrideGTRS()` / `resetGTRSToDefault()`
   - `loadGTRSImages()` 直接遍历 `resources.images.scene`
   - `getThemeAssetKey()` 三级优先级查找
   - 音频方法直接读 GTRS bgm/effect 配置
   - lint 检查：0 错误 ✅

3. **GTRS.json 内置默认主题**（上一轮已完成，本轮确认）
   - 路径已去掉 `/public/` 前缀
   - 字段顺序符合 GTRS 规范（specMeta 在前）

4. **public/gtrs-theme.json 静态文件修正**
   - 去掉所有 `/public/` 路径前缀
   - 音频格式统一为 `mp3`（与实际音频文件一致）
   - 补全 `login` / `ui` / `icon` / `effect` / `voice` 空分类字段

---

## 贪吃蛇 GTRS 规范适配 - 架构重构（第二轮）

### 设计决策变更
用户要求：GTRS.json 为纯占位符，游戏必须依赖后端主题，无主题时报错，删除 gtrs-theme.json

### 完成内容
1. **GTRS.json → 纯占位符**
   - 所有 src 置为空字符串
   - `$comment` 明确说明：仅供 TS 类型推导和 key 枚举，不可直接运行

2. **PhaserGame.ts 重构**
   - `GTRS` 初始值改为 `null`
   - 删除 `deepMerge()`、`resetGTRSToDefault()`、`GTRS_DEFAULT` import
   - `overrideGTRS` → `applyGTRS`（直接赋值，无兜底合并）
   - 新增 `assertGTRS()`：未加载时抛错
   - `loadTheme()` 任何失败都直接 `throw`（不 try/catch 静默）
   - `start()` themeId 必填，否则 `throw`
   - 所有 GTRS 访问点改为 `assertGTRS().xxx`

3. **theme.ts**
   - loadTheme 中 loadThemeFromBackend 失败时错误信息更明确

4. **删除 gtrs-theme.json**（public/games/.../themes/default/gtrs-theme.json）

---

## 贪吃蛇游戏 - 音频 416 错误修复（第三轮）

### 问题
用户报告 HTTP 416 (Requested Range Not Satisfiable) 错误，加载 `gameover.mp3` 失败。

### 根本原因
1. **所有音频和图片文件都是 0 字节** - 资源生成脚本输出到错误目录
   - 脚本输出到：`public/assets/themes/snake/audio/` 和 `public/assets/themes/snake/scene/`
   - 游戏加载路径：`public/games/snake-vue3/themes/default/audio/` 和 `public/games/snake-vue3/themes/default/images/scene/`
2. **StartView.vue 中的 `buildGTRSThemeJson` 是旧遗留代码**，生成的"假 GTRS"和真正从后端获取的 GTRS 格式不兼容

### 修复内容
1. **修复资源文件**
   - 运行 `generate-resources.mjs` 重新生成资源
   - 复制 WAV 音频到正确目录并创建 MP3 副本（复制 WAV 内容，扩展名为 .mp3）
   - 复制 PNG 图片到正确目录

2. **删除旧代码**
   - 删除 `StartView.vue` 中的 `buildGTRSThemeJson` 函数（约 140 行）
   - 移除 `validateGTRSTheme` 导入（不再需要）
   - 简化校验逻辑：直接在 `themeStore.gtrsRawJson` 中使用已校验的 GTRS 数据

3. **theme.ts 增强**
   - 新增 `gtrsRawJson` ref，存储从后端获取并校验通过的原始 GTRS JSON 字符串
   - 在 `loadThemeFromBackend` 成功后设置 `gtrsRawJson.value = configJsonStr`
   - 导出 `gtrsRawJson` 供 `StartView.vue` 使用

### GTRS 加载流程（严格模式）
1. `themeStore.init()` → 调用 `loadThemeFromBackend()`
2. 从后端 `/api/theme/download?id=xxx` 获取 configJson
3. `validateGTRSTheme()` 严格校验（Ajv Schema）
4. 校验通过 → 解析 JSON → 提取颜色到 `customTheme` → 存储原始 JSON 到 `gtrsRawJson`
5. `StartView.startGame()` → 直接使用 `themeStore.gtrsRawJson`（无需二次校验）
6. `PhaserGame.start()` → 再次从后端加载主题 → 校验 → `applyGTRS()`

### 路径规范化
`normalizeSrcPaths()` 函数自动处理 `/public/` 前缀：
- `/public/games/...` → `/games/...`
