# 2026-03-29 日志

## frame-factory 架构调整 v4.0 → v4.1

**问题**：之前设计的 `import { GridRenderer } from '@/frame-factory'` 会造成依赖耦合

**修正 v4.0**：
- ❌ 废弃：frame-factory/src 组件库 + 动态引用
- ✅ 新设计：frame-factory 只有 templates/game-template/，复制后游戏完全独立

**整合 v4.1**：
- ✅ 将 game-dev skill 的所有内容整合到 frame-factory
- ✅ 统一管理：文档、模板、脚本、SQL
- ✅ 删除不需要的 package.json

**最终结构**：
```
kids-game-frame-factory/
├── docs/                    # 开发文档
├── templates/               # 配置模板
│   └── game-template/       # 游戏项目模板
└── README.md               # 统一入口
```

详见 MEMORY.md 中的「游戏框架 v4.0 设计」
