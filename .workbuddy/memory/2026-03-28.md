# 2026-03-28 工作日志

## 配置参数未生效 bug 修复（2026-03-28）

**问题根因**：`GameSettingsPanel` 保存的 `currentGameConfig` 通过 `sessionStorage` 中转，但 `ComponentGameScene` 只读了 `difficulty/enableDynamicDifficulty`，而 `SnakePhaserGame`（`PhaserGame.ts`）完全忽略 sessionStorage，导致 `speed/initialLength/cellSize/分数` 全部无效。

**修复链路**：
1. **`types/game.ts`** — `DifficultyConfig` 新增 `initialLength` 字段，`DIFFICULTY_CONFIGS` 各难度补充 `initialLength`
2. **`stores/game.ts`** — 新增 `CustomGameConfig` 类型 + `customConfig: ref<CustomGameConfig | null>` + `setCustomConfig()` 方法；`startGameWithInit()` 优先读 `customConfig.initialLength`；`moveSnake()` 优先读 `customConfig.speed`；`generateFood()` 优先读 `customConfig.*FoodScore`
3. **`DifficultyView.vue`** — `startGame()` 改为：① `gameStore.setDifficulty(selectedDifficulty)` ② `gameStore.setCustomConfig(currentGameConfig)` ③ 不再走 sessionStorage；`selectedDifficulty` 初始值从 `gameStore.difficulty` 同步；去掉多余的 `gameStore.startGame()` 调用
4. **`SnakeGame.vue`** — 初始化时用 `startGameWithInit(cellSize)` 替代 `resetGame+startGame+generateFood`；`cellSize` 优先读 `customConfig.cellSize`（否则用 Phaser 自动计算值）

**生效的参数**：蛇初始长度、移动速度、格子大小、普通/奖励/特殊食物分数、各难度预设 initialLength

---

## uiResponsive.ts 响应式重构

**问题根因**：原来 `uiScale` 是模块级普通变量，`useResponsiveUI()` 返回的是值快照，resize 后 Vue computed 不会重新触发。

**重构方案**：
- `_screenW`/`_screenH` 改为 `ref()`，`uiScaleRef` 改为 `computed()`（模块级单例，全局共享）
- 所有 `getFontSize()`/`getPadding()` 等工具函数内部读取 `uiScaleRef.value`，在组件 `computed` 内调用时自动建立响应式依赖
- 全局 resize 监听统一在 `_ensureResizeListener()` 中注册（只注册一次），各视图无需再手动 `addEventListener/removeEventListener`

**修改文件**：
- `src/utils/uiResponsive.ts` — 核心重构
- `src/views/StartView.vue` — 移除 handleResize + addEventListener/removeEventListener
- `src/views/GameOverView.vue` — 修正 uiScale.value
- `src/views/DifficultyView.vue` — 移除冗余 uiScale computed，传 .value 给子组件
- `src/components/game/SnakeGame.vue` — 移除手动 resize 监听

**关键 API 变化**：
- `ui.uiScale` 原来是 number，现在是 `ComputedRef<number>`，模板中直接用 `ui.uiScale.value` 或在模板里 Vue 自动解包



**问题**：`DifficultyView.vue` 传给 `GameButton` 的 `fontSize`/`paddingLeft` 等 props 是 `ui.getFontSize()` 返回的带 `px` 字符串，但 `GameButton` 的 props 声明为 `number`（原始设计尺寸），内部自行缩放。导致 Vue warn。

**修复**：改为传字面量数字（如 `:fontSize="26"`），删除多余的 `btnFontSize`/`btnPaddingH`/`btnPaddingV` computed。

---

## kids-game-frame-factory 框架深度分析（2026-03-28 晚）

**发现的遗留问题（共 18 项）**：

| 优先级 | 问题 | 影响 |
|--------|------|------|
| 🔴 | `core/` vs `shared/core/` 双份实现，API 不同步 | 框架对外暴露 core/，内部用 shared/core/，两套并存 |
| 🔴 | `shared/game/PhaserGame.ts`（63KB）含 `@/` 别名 | 遗留文件，框架包无法单独编译 |
| 🔴 | `GameOrchestrator.ts` create/update 全为 TODO | 核心编排器是空壳 |
| 🔴 | `framework/` 只有 arcade，其余4个目录不存在 | 声称五种游戏类型，实际只有蛇 |
| 🔴 | `Direction` 类型在4处重复定义 | types/common、GridMovement、SnakeRenderer、arcade |
| 🔴 | `SnakeRenderer`/`FoodRenderer` 各有两份（shared/render/ vs shared/game/components/） | 版本冲突，定位混乱 |
| 🟡 | `shared/logic/` 混入蛇专有逻辑（FoodSpawnerComponent 直接依赖 SnakeSegment） | 破坏通用性 |
| 🟡 | `index.ts` 漏导出 shared/game/ 全部、utils 多个工具 | 包使用者无法访问 AudioManager 等 |
| 🟡 | VERSION 冲突（index.ts: 4.0.0 vs constants.ts: 1.1.0） | 版本信息混乱 |
| 🟡 | `SnakeSegment` 接口4处独立定义 | 类型不统一 |
| 🟡 | `GTRSTheme` 扩展类型3处重复（ResourceLoader、GTRSLoader、PhaserGame） | 代码重复 |
| 🟡 | `AudioManager` 两份（audio/ vs components/） | 重复组件 |
| 🟡 | `core/GameEvent.ts` 与 `shared/core/GameEvent.ts` 内容不同步 | 缺少 COLLISION_DETECTED |
| 🟡 | 渲染组件3个 TODO 方法（getThemeAssetKey 等返回 null） | GTRS主题注入未完成 |
| 🟢 | `shared/render/index.ts` ItemRenderer 悬空 TODO | 引用不存在的文件 |
| 🟢 | `ArcadeGameConfig` 在 arcade 目录内两处定义 | arcade-config.ts + ArcadeGameScene.ts |
| 🟢 | `constants.ts` 乱码字符（中文字符残缺） | 遗漏的乱码修复 |
| 🟢 | `shared/ui/` 目录不存在 | 架构文档声称有此目录 |

---

## kids-game-frame-factory 框架优化完成（TypeScript 编译零错误）

**修复文件（共 10 个）**：
1. `src/shared/render/GameObjectRenderer.ts` — 完全重写，修复二进制损坏 + `?)` 替换问题
2. `src/shared/render/BackgroundRenderer.ts` — 完全重写，修复乱码字符
3. `src/shared/render/FoodRenderer.ts` — 完全重写，修复乱码字符
4. `src/shared/render/GridRenderer.ts` — 完全重写，修复可选属性语法
5. `src/shared/render/ParticleRenderer.ts` — 完全重写，修复接口属性和方括号
6. `src/shared/render/SnakeRenderer.ts` — 完全重写，修复数组类型、下标访问等
7. `src/shared/utils/audioGenerator.ts` — 修复三元表达式（`)`→`?`）
8. `src/shared/utils/helpers.ts` — 修复正则表达式、数组下标、泛型约束
9. `src/shared/utils/platformApi.ts` — 修复接口可选属性、可选链
10. `src/shared/utils/audioGenerator.ts` — 已修复

**根本原因**：文件中文字符（`？`、`【`、`】`等）被编码为 `)`，方括号 `[` 被替换为 `)`，导致大量语法错误。

**验证结果**：`npx tsc --noEmit --skipLibCheck` 零错误通过 ✅

---

## 贪吃蛇难度选择页面排版优化

**修改文件**：
- `kids-game-house/games/snake/src/views/DifficultyView.vue` — 全面重构排版
- `kids-game-house/games/snake/src/components/ui/DifficultySelector.vue` — 优化卡片视觉
- `kids-game-house/games/snake/src/components/ui/GameSettingsPanel.vue` — 优化内嵌样式

**优化内容**：
1. **DifficultyView.vue** 全面重构为三段式布局：
   - 顶部：返回按钮 + 标题 + 副标题（渐变色标题）
   - 中部：可滚动区域，包含主题皮肤区块 + 难度选择区块 + 可折叠的高级设置
   - 底部：固定的"开始游戏"按钮栏（毛玻璃效果）
   - 去掉了原来多余的"返回"按钮，改为顶部圆形返回按钮
   - 高级设置通过折叠动画展开，使用 `<Transition>` 实现滑动效果

2. **DifficultySelector.vue** 改为响应式网格卡片：
   - 移动端单列，宽屏 ≥640px 自动变双列
   - 卡片新增：难度色标圆点 + 选中光晕背景 + 更清晰的参数标签
   - 选中效果：带颜色边框 + 对应颜色阴影
   - 更精细的 hover/active 动画

3. **GameSettingsPanel.vue** 优化内嵌样式：
   - 移除大标题（折叠按钮已承担标题角色）
   - 优化各区块 padding 和 border 样式
   - 操作按钮改为更紧凑的尺寸

---

## game-dev Skill 创建完成

**创建的文件**：
- `.workbuddy/skills/game-dev/SKILL.md` — 主入口文件
- `.workbuddy/skills/game-dev/docs/GAME_DEV_GUIDE.md` — 完整开发指南
- `.workbuddy/skills/game-dev/docs/GTRS_GUIDE.md` — GTRS 资源配置规范
- `.workbuddy/skills/game-dev/docs/CHECKLIST.md` — 开发检查清单
- `.workbuddy/skills/game-dev/templates/GTRS.template.json` — GTRS 配置模板
- `.workbuddy/skills/game-dev/templates/difficulty.template.json` — 难度配置模板
- `.workbuddy/skills/game-dev/templates/register-game.template.sql` — 数据库注册模板
- `.workbuddy/skills/game-dev/templates/generate-resources.template.mjs` — 资源生成脚本模板
- `.workbuddy/skills/game-dev/templates/i18n.template.json` — 国际化模板

**Skill 内容**：
- 整合 GTRS 规范、游戏克隆指南、屏幕适配方案
- 提供可复用的配置模板
- 包含详细的开发检查清单

---

## 项目清理完成（2026-03-28 晚）

**删除的内容**：
1. ✅ `kids-game-frame-factory/` 整个目录（组件化框架废弃）
2. ✅ 根目录测试/分析文档（6个）
   - CENTERING_FIX_REPORT.md
   - SNAKE_STARTVIEW_DEEP_ANALYSIS.md
   - TEST_GAME_*.md（4个）
3. ✅ `snake/` 游戏历史文档（100+ 个 .md）
4. ✅ `kids-game-house/docs/` 历史优化报告（6个）
5. ✅ `examples/` 临时目录

**保留的核心文件**：
- `kids-game-house/games/snake/` — 游戏源码和配置
- `kids-game-house/games/tank-battle/` — 第二个参考游戏
- `.workbuddy/skills/game-dev/` — 新开发指南 Skill
- `kids-game-house/docs/` — 有用的设计文档（README.md, DESIGN_REVIEW_CHECKLIST.md 等）
