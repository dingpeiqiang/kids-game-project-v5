# 2026-04-27 工作日志

## simple-game 太空射击游戏适配方案演进

### 第一次：等比缩放 + 黑边
- `Math.min(scaleByHeight, scaleByWidth)` → 宽屏手机有黑边
- 用户反馈：边界溢出

### 第二次：拉伸填充
- Canvas = 全屏尺寸，宽高独立缩放 `ctx.setTransform`
- 用户反馈：画面变形，不好看

### 第三次（最终）：Phaser ScaleManager ENVELOP 模式
- 用 Phaser 3 创建游戏实例，ScaleManager 处理缩放适配
- ENVELOP 模式：等比缩放填满屏幕，裁切超出部分，不变形无黑边
- 通过 CanvasTexture 保留原有 Canvas 2D 绘制代码

**修改文件**：
1. **spaceShooter.ts**：完全重写为 Phaser Scene
   - `SpaceShooterScene extends Phaser.Scene`
   - `preload()`: 创建 `CanvasTexture(400, 600)` 作为渲染目标
   - `create()`: 用 `this.add.image()` 显示 CanvasTexture
   - `update()`: 游戏逻辑 + `renderToCanvas()` 绘制到 CanvasTexture + `texture.refresh()`
   - 输入：Phaser pointer 事件，`screenToLogical()` 坐标转换
   - 安全区：SAFE_L/R/T/B 确保 HUD 不被裁切

2. **App.ts**：spaceShooter 分支简化为 `canvas.innerHTML = ''`

3. **main.css**：添加 `#phaser-space-shooter` 全屏容器样式

**Phaser 配置（最终方案）**：
```js
// 统一用 FIT 模式，等比缩放，画面完整不裁切
// 空白区域用深色背景 (#0a0a1e) 填充，视觉上与游戏背景融为一体
// PC 端：容器限最大 450×800
scale: {
  mode: Phaser.Scale.FIT,
  autoCenter: Phaser.Scale.CENTER_BOTH,
  autoRound: true,
}
```

**架构优势**：
- ✅ 不变形（等比缩放）
- ✅ 画面完整（FIT 不裁切）
- ✅ 空白区域深色背景，视觉一体
- ✅ PC 端画面清晰（限制容器尺寸）
- ✅ 保留原有 Canvas 2D 绘制代码
- ✅ 自动处理 DPI/resize/全屏

**踩坑**：
- 修复 `Phaser is not defined`：index.html 需添加 Phaser CDN `<script>`
- PC 端 ENVELOP → 400×600 放大到 1600px+ 画面模糊 → FIT + 限容器尺寸
- 手机端 ENVELOP → 左右内容被裁切显示不全 → 改为 FIT + 深色背景填充空白

## simple-game 首页游戏显示配置化

**新增文件**：`src/data/game-config.ts`

**功能**：通过配置控制首页显示哪些游戏、排序、角标
- `visible: boolean` — 是否显示（false 则首页不展示）
- `order: number` — 同分类内排序（越小越靠前）
- `badge: string` — 角标标签（"新"/"热门"/"推荐"/"限时"）

**⚠️ 关键逻辑**：未在 `GAME_DISPLAY_CONFIG` 中出现的游戏，默认 `visible=false`（不显示）
- 只有明确配置 `visible: true` 的游戏才会在首页展示
- 当前仅 `spaceShooter` 为 visible:true，其余被注释掉的游戏不会显示

**修改文件**：
1. **App.ts**：import game-config；renderGameCards 按 visible 过滤 + order 排序 + badge 角标；initRankGameSelector 和批量排名也过滤
2. **main.css**：添加 `.card-badge` 角标样式（渐变红橙，右上角）

## simple-game 首页加载性能优化

**问题**：24 个游戏卡片各有一个 `requestAnimationFrame` 动画循环同时运行，严重耗性能。

**优化**：
1. **IntersectionObserver 懒加载**：只有卡片进入视口才启动动画，离开视口停止
2. **静态首帧**：先绘制一帧静态画面，避免白屏等待
3. **RAF 生命周期管理**：`previewAnimFrames` Map 跟踪所有活跃动画，重渲染时先 `cancelAnimationFrame` 清理
4. **移除 setTimeout 逐帧渲染**：不再需要 `i * 80 + 100` 延迟

## spaceShooter 解压爽快升级

**核心改动**：让游戏更爽、更解压、更有打击感

### 新增系统
- **击穿弹（pierce）**：子弹穿透敌人不消失，一弹多杀
- **冲击波（shockwave）**：击杀大敌人/boss 产生扩散波纹
- **慢动作（slowMo）**：boss 击杀/清屏时短暂慢放，增强爽感
- **道具掉落（powerups）**：击杀敌人概率掉落道具，磁铁自动吸附
- **连击系统增强**：3连击以上显示 COMBO 文字，20连击以上文字变红加大

### 数值调整（旧 → 新）

| 参数 | 旧 | 新 | 说明 |
|------|----|----|------|
| 初始HP | 3 | 5 | 更耐打，更爽 |
| 最大HP | 3 | 5 | 配合更多回血 |
| 射击CD | 180ms | 120ms | 射速提升50% |
| 子弹速度 | 10 | 12 | 更快更爽 |
| 道具掉率 | 12-30% | 25-50% | 更频繁掉落 |
| 散弹基础数 | 5 | 7 | 更密集 |
| 敌人波次 | 每6波4只 | 每4波5只 | 更多敌人 |
| 爆炸粒子 | 小 | 1.5-2x | 更爽的爆炸 |

### 商店道具调整
- 新增：💥击穿弹（120分，15s CD）
- 全面降价：50-200分（旧80-500分）
- 冷却缩短：8-25s（旧10-40s）
- 道具栏高亮当前激活的道具

## spaceShooter Buff 平衡 & 性能优化

**问题**：buff 叠加太多 → 玩家无敌 → 无聊 + 后期卡顿

**修复**：

### 1. Buff 时长大幅缩短（不再叠加，只刷新）
| buff | 旧时长 | 新时长 |
|------|--------|--------|
| 三连发 | 10s+叠加 | 5s固定 |
| 扩散弹 | 8s+叠加 | 4s固定 |
| 速射 | 8s+叠加 | 4s固定 |
| 击穿弹 | 8s+叠加 | 4s固定 |
| 激光 | 8s+叠加 | 3s固定 |
| 无敌 | 3-15s叠加 | 3s固定 |
| 磁铁 | 10-50s | 5s固定 |

### 2. 同时激活 buff 上限 = 2
- `activeBuffOrder` 跟踪激活顺序
- 超限时移除最早激活的 buff
- heal/bomb/magnet/shield 不占 buff 位

### 3. 难度曲线提升
- 难度增长速度：15秒/级 → 10秒/级
- 敌人血量增长系数：0.8 → 1.2
- 敌人速度增长系数：0.15 → 0.2
- boss 出现：5级 → 4级
- hex 出现：3级 → 2.5级
- 敌人射击间隔缩短
- boss 弹幕更密（5发扇形）
- hex 双发散弹

### 4. 性能限制
| 对象 | 上限 |
|------|------|
| 子弹 | 30 |
| 敌弹 | 40 |
| 粒子 | 80 |
| 飘字 | 10 |
| 掉落物 | 8 |
| 道具掉率 | 15-30%（原25-50%）|

### 5. 射击不再叠加
- 三连发固定3发（不随 stacks 增加）
- 扩散固定7发
- 击穿固定穿透3
- 速射固定 0.4x CD

## spaceShooter 死亡结算卡住修复

**问题**：玩家死亡后页面卡住，结果弹窗不显示

**根因**：
1. `App.endGame()` 是 async，`await userService.recordGameResult()` 网络请求阻塞弹窗显示
2. `doEnd()` 提前 `phaserDiv.remove()`，但 `onEnd()` 中的 `phaserGame.destroy()` 还要操作已移除的 DOM
3. Phaser scene 死亡后仍在渲染循环中

**修复**：
1. **拆分 endGame**：`showResult()` 立即显示弹窗 + `syncScoreAsync()` 异步同步分数（不阻塞弹窗）
2. **doEnd() 不再重复 remove DOM**：交给 onEnd 中的 phaserGame.destroy() 统一处理
3. **死亡/通关时 `this.scene.pause()`**：停止 Phaser 渲染循环，释放 CPU
4. **后端排名异步更新**：syncScoreAsync 成功后更新弹窗中的排名显示

## spaceShooter 飞机不可见 + 黑边修复（4-29）

**飞机不可见 bug**：
- `drawPlayer()` 中 `else if (Math.floor(this.invincible * 10) % 2 === 0) { return }` 
- `invincible=0`（正常状态）→ `0 % 2 === 0` → true → return → 飞机不绘制！
- 修复：删除整个 else if 分支

**黑边优化**：
- 设计分辨率从 400×600（2:3）改为 360×640（9:16），贴近手机屏幕比例

**自动射击**：
- `update()` 中 `if (this.gameStarted)` 始终自动开火，不再需要 `mouseDown` 条件
- 移除 pointerdown/pointermove 中的手动 `this.shoot()` 调用
- 开始提示改为"🤖 自动射击！只需移动飞船！"

**通关/失败结算页统一化**：
- 通关（Lv10）和失败（HP=0）都走 `engine.endGame()` → `doEnd()` → `onEnd()` → `App.endGame()`
- 删除 canvas 内的 `drawVictoryScreen` 和 `handleVictoryClick`/`restartGame`
- 删除 `victoryButtonRect` 字段
- GameEngine 新增 `setVictory()/isVictory()` 标志通关
- App.endGame 根据 `isVictory` 显示"🎉 恭喜通关!"或"继续加油"

**结果弹窗移动端适配**：
- `result-card`: 宽度 92vw，底部 safe-area padding，max-height 90vh + overflow-y
- 按钮: min-height 48px，渐变色主按钮，触摸区域更大
- `replayGame/closeResult` 清理残留 Phaser DOM

**侧边道具栏移除**：
- 删除 `SHOP_ITEMS` 常量、`itemCooldowns`、`buyItem()`、`checkShopClick()` 
- 删除 pointerdown 中道具栏点击检测
- 删除道具栏绘制代码（左侧 8 个按钮）
- 删除冷却计时器更新逻辑
- **保留**：击杀掉道具 + 磁铁吸附 + 右下角 buff 状态显示
- 道具获取方式：纯靠击杀掉落拾取，无需手动操作
