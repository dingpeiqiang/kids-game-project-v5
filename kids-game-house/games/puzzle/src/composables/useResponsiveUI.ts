/**
 * @deprecated 请改用 `@/utils/uiResponsive`
 *
 * 此文件为向后兼容包装，统一重导出 utils 版本的响应式 UI 工具。
 *
 * 新代码请直接导入：
 * ```ts
 * import { useResponsiveUI, initUIParams } from '@/utils/uiResponsive'
 * ```
 */
export { useResponsiveUI, initUIParams, uiScaleRef } from '@/utils/uiResponsive'
