# 敌人坦克子弹方向修复

**时间**：2026-04-03 00:52
**问题**：敌人坦克的子弹方向与炮口方向不匹配

---

## 问题诊断

### 症状
敌人发射子弹时，子弹方向和炮口朝向不一致。

### 根本原因

**错误的射击逻辑**：
```typescript
// ❌ 错误：子弹朝向玩家（追踪玩家）
const angle = Phaser.Math.Angle.Between(enemy.x, enemy.y, player.x, player.y)
vx = Math.cos(angle) * bulletSpeed
vy = Math.sin(angle) * bulletSpeed
```

这种逻辑导致：
- 子弹朝玩家方向发射（追踪玩家）
- 与炮口方向无关
- 不是经典坦克大战的行为

---

## 修复方案

### 经典坦克大战的正确行为

**子弹应该沿着炮口方向发射**，而不是追踪玩家。

### 修复后的逻辑

```typescript
// ✅ 正确：子弹沿炮口方向（即敌人移动方向）
const body = enemy.body as any
const velocity = body.velocity

// 根据敌人的速度方向确定子弹方向
if (velocity.y < 0) {  // 敌人向上移动
  vx = 0
  vy = -bulletSpeed
} else if (velocity.y > 0) {  // 敌人向下移动
  vx = 0
  vy = bulletSpeed
} else if (velocity.x < 0) {  // 敌人向左移动
  vx = -bulletSpeed
  vy = 0
} else if (velocity.x > 0) {  // 敌人向右移动
  vx = bulletSpeed
  vy = 0
}
```

---

## 修改内容

### 文件
`src/managers/EnemyAIManager.ts` - `enemyShoot()` 方法

### 改动详情

**移除**：
- ❌ 计算敌人到玩家的角度
- ❌ 使用 `Phaser.Math.Angle.Between()` 追踪玩家

**添加**：
- ✅ 获取敌人的当前速度
- ✅ 根据速度方向确定子弹方向
- ✅ 子弹沿炮口方向发射
- ✅ 添加调试日志

---

## 修复效果

- ✅ 敌人向上移动时，子弹向上发射
- ✅ 敌人向下移动时，子弹向下发射
- ✅ 敌人向左移动时，子弹向左发射
- ✅ 敌人向右移动时，子弹向右发射
- ✅ 子弹方向始终与炮口方向一致
- ✅ 符合经典坦克大战游戏行为

---

## 测试建议

1. **测试四方向发射**：
   - 让敌人向上移动，观察子弹是否向上发射
   - 让敌人向下移动，观察子弹是否向下发射
   - 让敌人向左/右移动，观察子弹方向

2. **测试转向后发射**：
   - 敌人转向后，观察子弹方向是否跟随新方向

3. **测试静止时发射**：
   - 敌人静止时，默认向下发射

---

## 后续优化建议

1. **增强射击逻辑**（可选）：
   - 检测前方是否有障碍物，避免贴墙射击
   - 添加射击冷却时间，避免连续快速射击

2. **子弹优化**（可选）：
   - 子弹从炮口位置发射，而不是坦克中心
   - 添加子弹发射音效

---

## 总结

本次修复的核心是改变敌人的射击逻辑：**从"追踪玩家"改为"沿炮口方向"**，使子弹方向始终与炮口方向一致，符合经典坦克大战游戏的标准行为。
