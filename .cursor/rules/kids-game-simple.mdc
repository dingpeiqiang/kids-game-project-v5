---
description: kids-game-simple 主工程开发约定（2D Phaser / 3D Babylon）
globs: kids-game-simple/**
alwaysApply: false
---

# kids-game-simple 开发规则

完整规范见仓库根目录 [AGENTS.md](mdc:AGENTS.md)；本文件为 Cursor 速查。

## 范围
- 只改 `kids-game-simple/` 交付；勿把 `games/<name>/` 路径套到 monorepo 根目录。
- 2D **保留**；3D **Babylon 重做**，**禁止**新增或大面积修补 Three.js 3D。

## 接入游戏
- 注册：`src/games/gameRegistry.ts` → `GAME_REGISTRY` + `GAME_DISPLAY_CONFIG`
- 同步：`src/data/games.ts` 的 id / 文案
- 签名：`init(engine: GameEngine, onEnd: () => void)`；WebGL/Phaser **必须** `destroy`
- 未完成 3D：`visible: false`，无「新」角标

## 目录
- `src/games/<gameId>/`（camelCase，与 id 一致）
- 复杂游戏拆 `logic/`、`render/` 或 `scenes/`；单文件建议 ≤500 行（纯数据 config 可放宽）

## 禁止
- 根目录或游戏目录新增 `*_FIX_REPORT.md` / `*_COMPLETE.md`
- 局内用 `setTimeout` 刷怪/无敌（仅壳层回大厅可用）
- 元数据只改 `games.ts` 不改 Registry

## 验证
- `cd kids-game-simple && npm run type-check`